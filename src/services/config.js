export const firebaseConfig = {
    // go to console.firebase.google.com -> projectU -> settings icon -> 
    // general -> scroll down to your apps -> Firebase SDK snippet -> Config
    // copy the inside of firebaseConfig here and yarn start
};